<!DOCTYPE html>
<html>
  <head>
    <title>FamilyDirectory</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <!-- jquery.com -->
    <%= javascript_include_tag 'https://code.jquery.com/jquery-3.6.4.min.js' %>
    <%= javascript_importmap_tags %> 
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-md" style="background-color: #b7d5ea">
        <%= render 'layouts/navbar' %>
      </nav>
    </header>
    <main>
      <div class="container-fluid" id="container">
        <%= render 'layouts/flash' %> <%= yield %>
      </div>
    </main>
    <footer
      class="container-fluid d-flex justify-content-center ps-2 pe-2"
      style="background-color: #b7d5ea; height: 55px; align-items: center"
    >
      <%= render 'layouts/footer' %>
    </footer>
  </body>
</html>
