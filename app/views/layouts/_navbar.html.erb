<nav class="navbar navbar-expand-lg" style="background-color: #b7d5ea">
  <div class="container-fluid">
    <%= link_to "Family Directory", root_path, class: "navbar-brand" %>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav  d-flex justify-content-end w-100">
        
      <% if user_signed_in? %>
      <li class="nav-item">
      <%= link_to "Contacts", contacts_path, class: "btn navbar-link"  %>
      </li>
       <li class="nav-item">
      <%= link_to "Wishlist", wishlists_path, class: "btn navbar-link"  %>
      </li>
      <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
             <%=current_user.first_name%> <%=current_user.last_name%>
          </a>
        <ul class="dropdown-menu">
            <li><%= link_to "Edit profile", edit_user_registration_path, class: "btn dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn dropdown-item"  %></li>
          </ul>
      </li>
        <% else %>
           <li class="nav-item">
         <%= link_to "Login", new_user_session_path, class: "btn navbar-link"  %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign up", new_user_registration_path, class: "btn navbar-link"  %>
        </li> 
        <% end%>
      </ul>
    </div>
  </div>
</nav>
